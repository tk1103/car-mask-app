# Vercel環境でのトラブルシューティング（Carkusu）

## 確認事項

### 1. 環境変数の設定確認

Vercelダッシュボードで以下を確認してください：

1. **プロジェクトのSettings > Environment Variables**を開く
2. `GEMINI_API_KEY`が設定されているか確認
3. 以下の環境すべてにチェックが入っているか確認：
   - ✅ Production
   - ✅ Preview
   - ✅ Development

**設定方法：**
- Name: `GEMINI_API_KEY`
- Value: あなたのGemini APIキー（`AIza...`で始まる文字列）
- Environment: すべてにチェック

### 2. 再デプロイの実行

環境変数を変更した場合は、**必ず再デプロイ**が必要です：

1. Vercelダッシュボードで「Deployments」タブを開く
2. 最新のデプロイメントの「...」メニューをクリック
3. 「Redeploy」を選択
4. または、GitHubにプッシュして自動デプロイをトリガー

### 3. Vercelのログを確認

1. Vercelダッシュボードで「Deployments」タブを開く
2. 最新のデプロイメントをクリック
3. 「Functions」タブを開く
4. `/api/detect-plate`をクリック
5. 「Logs」タブでエラーログを確認

**確認すべきログ：**
- `[detect-plate] Gemini API timeout` - タイムアウト
- `[detect-plate] Gemini API error` - APIエラー
- `[detect-plate] Success:` - 成功時の応答時間

### 4. ブラウザのコンソールを確認

1. アプリを開く（例: `https://your-app.vercel.app`）
2. ブラウザの開発者ツールを開く（F12）
3. 「Console」タブを開く
4. 撮影してナンバー検出を実行
5. エラーメッセージを確認

**確認すべきエラー：**
- `[client] API fetch timeout` - クライアント側タイムアウト
- `[client] API fetch error` - 通信エラー
- 解析サーバが混雑しています - Gemini側の過負荷

### 5. よくある問題と解決方法

#### 問題1: APIキーが設定されていない

**症状：**
- エラーメッセージ: "GEMINI_API_KEYが設定されていません"

**解決方法：**
1. Vercelの環境変数で`GEMINI_API_KEY`を設定
2. 再デプロイを実行

#### 問題2: 解析がタイムアウトする

**症状：**
- 「解析サーバが混雑しています」または「解析に時間がかかりすぎました」

**解決方法：**
1. しばらく時間をおいて再試行
2. 画像を撮り直す（プレートがはっきり見えるように）
3. 通信環境を確認

#### 問題3: 1日の利用制限に達した

**症状：**
- 「本日の利用制限に達しました」

**解決方法：**
1. 本アプリは1日20回まで（IPごと）。翌日まで待つ

### 6. サポートが必要な場合

以下の情報を提供してください：

1. **Vercelのログ**（`/api/detect-plate`のログ）
2. **ブラウザのコンソールエラー**
3. **環境変数の設定状態**（APIキーは含めない）
4. **発生しているエラーメッセージ**

## 次のステップ

1. 環境変数を確認・設定
2. 再デプロイを実行
3. ログを確認
4. エラーメッセージに基づいて対処
